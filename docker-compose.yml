version: "3"
services:

  frontend:
    ports:
      - "8000:8000"
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      - backend
    environment:
      -  APPID:${APPID}

  backend:
    ports:
      - "9000:9000"
    build:
      context: ./backend
      dockerfile: Dockerfile
